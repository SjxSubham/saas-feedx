name: AST Structural Scan

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  ast_grep_scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run ast-grep Scan
        uses: ast-grep/action@v1
